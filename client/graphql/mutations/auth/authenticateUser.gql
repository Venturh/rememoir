mutation authenticateUser($email: String!, $password: String!) {
  login(input: { email: $email, password: $password }) {
    user {
      email
      verified
      id
    }
    accessToken
    errors {
      field
      message
    }
  }
}
