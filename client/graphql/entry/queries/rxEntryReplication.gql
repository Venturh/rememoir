query rxEntryReplication(
  $lastId: String!
  $minUpdatedAt: String!
  $limit: Float!
) {
  rxEntryReplication(
    lastId: $lastId
    minUpdatedAt: $minUpdatedAt
    limit: $limit
  ) {
    id
    contentText
    contentUrl
    contentType
    contentPreview {
      ogTitle
      ogDescription
      ogUrl
      ogImage
    }
    categories
    calendarDate
    processing
    updatedAt
    hashedKey
    deleted
  }
}
